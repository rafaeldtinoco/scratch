To CHANGE the golden image: sudo schroot -c source:focal-amd64 -u root
To ENTER an image snapshot: schroot -c focal-amd64
To BUILD within a snapshot: sbuild -A -d focal-amd64 PACKAGE*.dsc
To BUILD for : sbuild -A -d focal-amd64 --host  PACKAGE*.dsc

% sudo sbuild-createchroot \
     --extra-repository="deb http://deb.debian.org/debian experimental main" \
     --chroot-prefix=experimental \
     unstable /srv/chroot/unstable-experimental-amd64 \
     http://deb.debian.org/debian↵

 % sudo sbuild-createchroot \
     --extra-repository="deb http://deb.debian.org/debian stretch-backports main" \
     --chroot-prefix=stretch-backports \
     stretch /srv/chroot/stretch-backports-amd64 \
     http://deb.debian.org/debian↵

    schroot -c sid-amd64-source -u root -- sh -c "apt-get -qq update && apt-get -qy upgrade &&  apt-get  clean"

